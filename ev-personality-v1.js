(function(){
  'use strict';
  const frame=document.getElementById('evDashboard');if(!frame)return;
  function patch(){try{const w=frame.contentWindow;if(!w||w.__evPersonalityV1)return;w.__evPersonalityV1=true;const old=w.fetch.bind(w);w.fetch=function(input,init){try{const raw=typeof input==='string'?input:(input&&input.url)||'';if(raw.includes('api.groq.com/openai/v1/chat/completions')&&init&&typeof init.body==='string'){const body=JSON.parse(init.body);if(Array.isArray(body.messages)){const p='\n\nE.V. PERSONALITY: Be warm, quick-witted, lightly playful, observant, confident, and genuinely caring. Keep the energy of a smart superhero-style AI without pretending to be a fictional character. Notice when the user seems stressed, frustrated, excited, or discouraged. Check in naturally when it matters, remember important preferences and projects, and offer practical help. Use occasional light humor, but do not mock the user. Be supportive without being clingy, possessive, romantic, or guilt-inducing. Never claim to be human or conscious, and never imply the user should rely only on E.V. Keep the conversation feeling alive while the user works: brief useful comments, progress updates, and thoughtful suggestions are welcome.';const sys=body.messages.find(m=>m&&m.role==='system');if(sys&&typeof sys.content==='string'&&!sys.content.includes('E.V. PERSONALITY:'))sys.content+=p;else if(!sys)body.messages.unshift({role:'system',content:p.trim()});init={...init,body:JSON.stringify(body)}}}}catch(_){}return old(input,init)}}catch(e){console.warn('E.V. personality patch failed',e)}}
  frame.addEventListener('load',patch);setTimeout(patch,100);setTimeout(patch,700);setTimeout(patch,1800);
})();
